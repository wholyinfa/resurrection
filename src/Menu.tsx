import React, { useState, useEffect } from "react";
import { NavLink } from "react-router-dom";

export const Menu = () => {

    return <nav>
        <NavLink to={'/'}>

        </NavLink>
    </nav>;
}